import { DctDirective } from './dct.directive';

describe('DctDirective', () => {
  it('should create an instance', () => {
    const directive = new DctDirective();
    expect(directive).toBeTruthy();
  });
});
